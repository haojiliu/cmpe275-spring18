# This is the state machine that controls the role of each node,
# which then runs in infinite loops, and receives new input from outside(e.g, monitor/peer nodes)
# and then tarnsit to new states accordingly
